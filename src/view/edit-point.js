/* eslint-disable camelcase*/
import {createProtoPoint} from './proto-point.js';

export const createEditionPoint = (date) => `
  ${createProtoPoint(date)}
        </section>
      </section>
    </form>
  </li>`;
